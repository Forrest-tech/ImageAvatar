{
  "ImageAvatar": {
    // POD environment overrides.
    // Leave blank to use per-user defaults (%Documents%\ImageAvatar).
    // These values are only applied on a fresh install (before the user
    // saves their own settings to %AppData%\ImageAvatar\settings.json).
    "WorkspaceRoot":   "",
    "ModelPath":       "",
    "TemplatesFolder": ""
  },

  "Logging": {
    "LogLevel": {
      "Default":                   "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  }
}
