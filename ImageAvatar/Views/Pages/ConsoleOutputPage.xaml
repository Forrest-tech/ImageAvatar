<UserControl
    x:Class="ImageAvatar.Views.Pages.ConsoleOutputPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:ImageAvatar.ViewModels"
    mc:Ignorable="d"
    Background="#1C1C1E"
    d:DataContext="{d:DesignInstance Type=vm:ConsoleViewModel}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0"
                Background="#252527"
                BorderThickness="0,0,0,1"
                BorderBrush="#404042"
                Padding="12,6">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{DynamicResource NavConsole}"
                           Foreground="#AAAAAA"
                           FontSize="12"
                           VerticalAlignment="Center"
                           Margin="0,0,16,0" />
                <Button Content="{DynamicResource BtnClearLog}"
                        Command="{Binding ClearCommand}"
                        Background="#3A3A3C"
                        Foreground="#CCCCCC"
                        BorderBrush="#555555"
                        BorderThickness="1"
                        Height="24"
                        Padding="10,0"
                        FontSize="12"
                        Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="2"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#4A4A4C" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Border>

        <!-- Log output -->
        <ScrollViewer Grid.Row="1"
                      x:Name="LogScroll"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Auto"
                      Background="#1C1C1E"
                      Padding="12,8">
            <ItemsControl ItemsSource="{Binding Entries}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}"
                                   FontFamily="Consolas"
                                   FontSize="12"
                                   Foreground="#CCCCCC"
                                   Margin="0,1" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
