<UserControl
    x:Class="ImageAvatar.Views.Pages.GlobalConfigPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vm="clr-namespace:ImageAvatar.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=vm:GlobalConfigViewModel}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="280" />
            </Grid.ColumnDefinitions>

            <!-- ── Left: parameters ── -->
            <StackPanel Grid.Column="0">

                <TextBlock Text="全局配置" FontSize="22" FontWeight="SemiBold"
                           Margin="0,0,0,20" />

                <ui:Card Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="文件名配置" FontSize="14" FontWeight="SemiBold"
                                   Margin="0,0,0,12" />

                        <CheckBox Content="全局文件名使用 SPU"
                                  IsChecked="{Binding UseSpuFilename}"
                                  Margin="0,0,0,12" />

                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="SPU 前缀" VerticalAlignment="Center"
                                       Width="80" Margin="0,0,8,0" />
                            <ui:TextBox Text="{Binding SpuPrefix, UpdateSourceTrigger=PropertyChanged}"
                                        Width="120"
                                        IsEnabled="{Binding UseSpuFilename}" />
                        </StackPanel>
                    </StackPanel>
                </ui:Card>

                <ui:Button Content="保存配置"
                           Appearance="Primary"
                           Command="{Binding SaveCommand}"
                           HorizontalAlignment="Left" />
            </StackPanel>

            <!-- ── Right: info panel ── -->
            <ui:Card Grid.Column="2" VerticalAlignment="Top">
                <StackPanel>
                    <TextBlock Text="说明" FontSize="13" FontWeight="SemiBold"
                               Margin="0,0,0,8" />
                    <TextBlock TextWrapping="Wrap"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                               Text="启用「全局文件名使用 SPU」后，所有输出文件将以 SPU 前缀（默认 N）加流水号命名，便于与 ERP 系统对接。&#x0a;&#x0a;SPU 前缀区分大小写，建议使用大写字母。" />
                </StackPanel>
            </ui:Card>
        </Grid>
    </ScrollViewer>
</UserControl>
