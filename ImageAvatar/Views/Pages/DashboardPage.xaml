<ui:Page
    x:Class="ImageAvatar.Views.Pages.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:vm="clr-namespace:ImageAvatar.ViewModels"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=vm:DashboardViewModel}"
    Scrollable="True">

    <Grid Margin="24,16,24,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Page Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <ui:TextBlock
                Text="{DynamicResource NavDashboard}"
                FontTypography="TitleLarge"
                Margin="0,0,16,0" />
            <ui:Button
                Content="{DynamicResource BtnRefresh}"
                Command="{Binding RefreshCommand}"
                Icon="{ui:SymbolIcon Symbol=ArrowSync24}"
                Appearance="Secondary"
                VerticalAlignment="Center" />
            <ui:ToggleButton
                Content="{DynamicResource BtnWatch}"
                IsChecked="{Binding IsWatching}"
                Command="{Binding ToggleWatchingCommand}"
                Margin="8,0,0,0"
                VerticalAlignment="Center" />
        </StackPanel>

        <!-- Root Path Configuration -->
        <ui:Card Grid.Row="1" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ui:TextBlock
                    Grid.Column="0"
                    Text="{DynamicResource LabelRootPath}"
                    VerticalAlignment="Center"
                    Margin="0,0,12,0" />
                <ui:TextBox
                    Grid.Column="1"
                    Text="{Binding RootPath, UpdateSourceTrigger=PropertyChanged}"
                    PlaceholderText="C:\Work\ImageAvatar"
                    VerticalAlignment="Center" />
                <ui:Button
                    Grid.Column="2"
                    Content="{DynamicResource BtnApply}"
                    Command="{Binding ApplyRootPathCommand}"
                    Appearance="Primary"
                    Margin="8,0,0,0"
                    VerticalAlignment="Center" />
            </Grid>
        </ui:Card>

        <!-- Pipeline Folders Grid -->
        <ItemsControl Grid.Row="2" ItemsSource="{Binding FolderItems}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="3" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:FolderStatusItem}">
                    <ui:Card Margin="6">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <!-- Stage color indicator + role icon -->
                            <Grid Grid.Row="0" Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Border
                                    Grid.Column="0"
                                    Height="4"
                                    CornerRadius="2"
                                    HorizontalAlignment="Left"
                                    Width="40"
                                    Background="{Binding StageColor}" />
                                <TextBlock
                                    Grid.Column="1"
                                    Text="{Binding RoleIcon}"
                                    FontSize="18"
                                    VerticalAlignment="Center" />
                            </Grid>

                            <!-- Folder name -->
                            <ui:TextBlock
                                Grid.Row="1"
                                Text="{Binding FolderName}"
                                FontTypography="BodyStrong"
                                Margin="0,0,0,4" />

                            <!-- File count -->
                            <StackPanel Grid.Row="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,4,0,8">
                                <ui:TextBlock
                                    Text="{Binding FileCount}"
                                    FontSize="32"
                                    FontWeight="Bold"
                                    Foreground="{Binding StageColor}" />
                                <ui:TextBlock
                                    Text=" PNG"
                                    FontSize="14"
                                    VerticalAlignment="Bottom"
                                    Margin="4,0,0,4"
                                    Opacity="0.6" />
                            </StackPanel>

                            <!-- Open folder button -->
                            <ui:Button
                                Grid.Row="3"
                                Content="{DynamicResource BtnOpenFolder}"
                                Command="{Binding DataContext.OpenFolderCommand,
                                    RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                CommandParameter="{Binding FullPath}"
                                Icon="{ui:SymbolIcon Symbol=FolderOpen24}"
                                Appearance="Secondary"
                                HorizontalAlignment="Stretch" />
                        </Grid>
                    </ui:Card>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</ui:Page>
